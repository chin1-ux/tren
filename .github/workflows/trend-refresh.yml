name: Trend Refresh (Main Account - 8h)

on:
  schedule:
    # Main Account: Run trend refresh every 8 hours (04:00, 12:00, 20:00 UTC)
    - cron: '0 4,12,20 * * *'
  workflow_dispatch: # Allows manual trigger from GitHub UI

concurrency:
  group: trend-refresh-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run-refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install .[worker]

      - name: Run trend refresh
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GROQ_API_KEY_2: ${{ secrets.GROQ_API_KEY_2 }}
          GROQ_API_KEY_3: ${{ secrets.GROQ_API_KEY_3 }}
        run: |
          python backend/manual_run_and_check.py --stages schema,refresh,snapshots
