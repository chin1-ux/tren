providers = ["python"]

[phases.setup]
nixPkgs = ["...", "libsndfile", "ffmpeg"]

[phases.install]
cmds = [
  "python3 -m venv /opt/venv",
  "/opt/venv/bin/pip install -r backend/requirements.txt"
]

[start]
cmd = "/opt/venv/bin/python backend/cron_job.py"


