[phases.setup]
nixPkgs = ["...", "python3"]

[phases.install]
cmds = [
  "python3 -m venv .venv",
  ".venv/bin/pip install -r backend/requirements.txt"
]

[start]
cmd = ".venv/bin/python backend/cron_job.py"

