{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && npm install && npm run build && cp -r .vercel/output ../.vercel/output",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://trendrop-backend.onrender.com/api/$1"
    },
    {
      "source": "/outputs/(.*)",
      "destination": "https://trendrop-backend.onrender.com/outputs/$1"
    }
  ]
}
