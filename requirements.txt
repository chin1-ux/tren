# Core API deps — keep lightweight for Vercel serverless
fastapi
uvicorn
requests
python-dotenv
supabase
slowapi
pydantic
email-validator
python-multipart
schedule
resend
google-generativeai
apify-client
psycopg2-binary
razorpay
redis
rq
# NOTE: librosa/scipy/moviepy/soundfile are NOT listed here — they are
# only used by local workers (beat_detector, reel_generator) and would
# pull in numba which fails to build on Python 3.12+.
